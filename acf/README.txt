Unorganized notes to come back to


Setup instructions
- define ACF_CONFIG_DIR in bashrc

Note that sequences with parameters are different values from another parameter
with the same name that is defined in the experiment.
